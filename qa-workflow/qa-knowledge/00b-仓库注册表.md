# 00b 仓库注册表（实例）

> 实例化自 `templates-v2/00b-仓库注册表.md`，2026-09-07 全仓普查（元数据 + 结构 + 远端分支核验）。复刻 Codex 首日仓库扫描并升级为注册表资产。

## 仓库清单（本地检出）

| 仓库 | 角色 | 版本 | 本地 HEAD | 远端 dev HEAD | ⚠️偏差 | 规模 | 技术栈 |
|------|------|------|-----------|---------------|--------|------|--------|
| `[app-]audio/[company-]network_flutterapp` | Flutter 壳工程（"Pure Flutter app"） | 5.3.0+32 | dev@4d1ed7b1 | **dev@8a628cb8** | **落后远端** | 360MB；lib 仅 3 个 dart 文件（纯壳实证） | Flutter/Dart，12 依赖（2 内部包） |
| `[app-]audio/[company-]network_flutter_core` | 共享业务核心（"Shared Flutter business core"） | 5.2.0+30 | dev@402853ab | **dev@4413fe40** | **落后远端** | 170MB；lib 839 dart / test 421 dart | Flutter/Dart，64 依赖（11 内部包） |

**分支策略**：`dev` 主开发线 / `master` 发布线 / `harmony` **鸿蒙移植线**（两仓均有）/ `localagent`（仅 Core）。

**⚠️ 基线偏差警示**：本地检出为 9/1 快照，远端 dev 已前进（App +1 提交、Core +1 提交）。本地工作树另有未提交修改（App 3 行 / Core 2 行，analysis_options+pubspec.lock 既有改动）。**是否 pull 追平由主对话/用户裁决**——追平则模块卡片绑定基线过期检测将触发全量告警。

## 仓库边界与缺口

- 账号（[tester]）当前**仅此 2 仓权限**（与 09-02 Codex 结论一致）；`mobile-native/`、`server/` 目录为空
- 基线矩阵中 **[COMPANY] Network-Android / FlutterModule / iOS / iOS-Framework / Server / Server-AI** 未检出——待权限开放或按需克隆
- 群组（[GIT_SERVER]/[target-app]，实际子组路径 [target-app]/[app-]audio）全量项目清单需 GitLab API token 才能枚举——**待补**

## 无形资产（考古/测试可直接利用）

| 资产 | 位置 | 价值 |
|------|------|------|
| **[company-]app-e2e**（自带 pytest E2E 框架！） | FlutterApp 仓，88 文件（conftest/pytest.ini/pages/flows/configs） | 现成的 Page Object E2E 套件——我们的执行从可复用其页面定位与登录态管理 |
| integration_test 冒烟 ×2 | FlutterApp `integration_test/`（auth_login_logout、wifi_device） | Patrol/integration_test 白盒基线 |
| 功能清单 | FlutterApp `docs/funtion_list/` | 用例拆分的官方输入 |
| 自动化测试指南 | FlutterApp `docs/automation_test_guide.md` | 与我们工作流的对接点，待考古从研读 |
| 13 个嵌套包 | Core `packages/`（upnp/ble/ohos蓝牙发现/AI音频/支付/三方登录/音量/local_audio/music_platform…） | 模块卡片拆分天然边界；`[app-]bluetooth_discovery_ohos` 印证鸿蒙线 |
| 产品文档 | Core `docs/prd/`、`testing.md`、`subwoofer/` | 业务要求映射（00d）的输入源 |

## 副本治理（09-02 恢复事件遗留）

`[company-]network_flutter_core.recovered{,2,3,4}-20260902` 与 `[COMPANY] Network-FlutterApp.validation-20260902` 共 5 个重复检出（约 900MB）。注意：**recovered 副本含完整 406 提交历史，主检出仅 1 提交（快照式）**——若需要完整历史应从 recovered 副本取。处置建议（待用户裁决）：确认无引用后归档至 `G:\[TARGET_APP]-Data\quarantine\` 或删除。

## 接入状态

| 仓库 | 接入状态 | 下一步 |
|------|----------|--------|
| FlutterApp | ✅ 已接入（L1/L2 绿，登录考古完成） | 基线追平裁决；[company-]app-e2e 对接评估 |
| Core | ✅ 已接入（门禁阻塞：328 诊断+43 失败） | CORE-T1~T6 |
| 其余 6+ 仓 | ⬜ 未接入（无权限/未检出） | 权限申请或明确排除 |
