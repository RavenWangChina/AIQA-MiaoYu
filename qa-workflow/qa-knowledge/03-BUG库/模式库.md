# BUG 模式库

> 每个 bug 归档时同步更新（实例化自 `docs/workflow/qa/templates-v2/03-模式库.md`，格式沿用上游 AIQA-MiaoYu 模式库模板）。反哺规则：同特征 bug 第二次出现，逆向用例从必须加针对性用例（设计文档第 6 节硬规则 4）。

| 模式编号 | 特征 | 高发模块 | 出现次数 | 反哺动作 | 相关 BUG |
|----------|------|----------|----------|----------|----------|
| P-001 | 契约不同步类：接口/Port 声明新增方法后，测试替身（_Fake*）未同步实现，analyzer/测试级联报错 | Flutter Core platform_ports | 2 | 逆向用例从加"接口演进后替身契约同步"检查项；考古从在模块卡片"已知坑"记录 Port 与替身的对应关系 | BUG-20260904-002, BUG-20260904-003 |
| P-002 | 嵌套包依赖边界类：packages/ 下嵌套包的 dev 依赖（package:test）未声明或解析失败，符号级联缺失 | Flutter Core [company-]upnp_core | 1 | 逆向用例从加"嵌套包独立构建与依赖声明"检查项；出现第二次即升级为独立排查任务 | BUG-20260904-001 |
| P-003 | 启动时序 ANR 类：首次启动主线程阻塞，系统弹 "isn't responding" 对话框，UI 层级暂不可达 | [APP_PACKAGE] 启动/MainActivity | 1（候选，未稳定复现） | 执行从进业务用例前先做启动 ANR 探针（hierarchy 可达性检查）；未稳定复现前不提单 | BUG-20260904-004 |
