# Risks

- Core is not L1/L2 green: analyzer exit 1 and 43 failing tests block downstream APK or device validation.
- The worktrees were not clean at task start (`analysis_options.yaml`, `pubspec.lock`); repository modifications cannot be attributed solely to this task without the prior diff artifact.
- `pub get` may rewrite lockfiles; the observed dirty lockfile state must be reviewed before any commit or merge.
- Core test output is large and includes platform-channel stubs (OHOS/iOS/Android) and widget timing assumptions; failures should be triaged by category before code changes.
- App green status depends on the current local Core path and resolved package cache; reproduce with the same Flutter executable and cache environment.
- Validation used synthetic/local test data only; no production service, real device, or APK build was exercised.
